# Calibration

A tiny spec to confirm the model server is reachable and that the coordinate
space matches the served checkpoint before running real flows. Point it at any
page with an obvious, unambiguous target.

## Localize and confirm a known element
- goto /
- verify the page has loaded
- screenshot
- click the main heading at the top of the page
- verify something on the page is visible
